{
  "name": "luvus-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "copy-scripts": "cp ../install.sh ../install.ps1 public/",
    "predev": "npm run copy-scripts",
    "prebuild": "npm run copy-scripts",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.36.0",
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/orbit": "^5.3.0",
    "astro": "^5.0.0",
    "sharp": "^0.33.0"
  }
}
